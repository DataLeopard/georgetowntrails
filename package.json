{
  "name": "trail-tracker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "creates map of route driven and saves and displays with collapsable menu for   days weeks or months of trails and tracking for uniqque display views useful for overall wide perspective",
  "homepage": "https://DataLeopard.github.io/georgetowntrails",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
